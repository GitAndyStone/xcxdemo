<!--flappy-bird.wxml-->
<view class="container log-list">
  <div id="game_div"> </div>
    <block wx:for="{{logs}}" wx:for-item="log">
    <text class="log-item">{{index + 1}}. {{log}}</text>
  </block>
</view>
